# Rime schema settings
# encoding: utf-8
__include: cosmos_svnpin_port.schema:/

schema:
  name: "笔画键盘"
  schema_id: cosmos_stroke
  description: 笔画键盘

speller:
  algebra:
    - xform/^[a-z]+([HSPNZ]+)$/$1/
    - xlit/HSPNZ/CVBNM/

translator:
  prism: cosmos_stroke

style:
  layout:
    sticky_lines: 6
    max_entries: 6
  keyboards: [.default]
